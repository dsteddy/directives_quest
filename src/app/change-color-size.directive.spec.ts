import { ChangeColorSizeDirective } from './change-color-size.directive';

describe('ChangeColorSizeDirective', () => {
  it('should create an instance', () => {
    const directive = new ChangeColorSizeDirective();
    expect(directive).toBeTruthy();
  });
});
